import { Link } from 'react-router-dom';
import { ArrowRight } from 'lucide-react';
export default function OpportunityListItem({ opportunity }) {
  const reviewState = opportunity.review?.status === 'COMPLETE' && opportunity.review?.valid ? 'Review complete' : opportunity.review?.status === 'INVALIDATED' ? 'Invalidated' : 'Unreviewed';
  return <li className="coi-opportunity-row"><span className="coi-rank">{opportunity.rank}</span><div><strong>{opportunity.business?.business_name || 'Unknown business'}</strong><small>{opportunity.business?.domain || 'Domain unavailable'}</small></div><div><span className="coi-label">Confidence</span><strong>{opportunity.confidence}</strong></div><div><span className="coi-label">Recommended offer</span><strong>{opportunity.recommendation?.title || 'Unavailable'}</strong></div><div><span className="coi-label">Review state</span><span className={`coi-status ${reviewState === 'Review complete' ? 'success' : reviewState === 'Invalidated' ? 'danger' : 'warning'}`}>{reviewState}</span></div><div><span className="coi-label">Next action</span><strong>{opportunity.next_action?.type || (opportunity.outreach_eligible ? 'Choose action' : 'Complete review')}</strong></div><Link className="coi-row-link" to={`/opportunities/${opportunity.workspace_id}`} state={{ preserveList: true }} aria-label={`Open ${opportunity.business?.business_name || 'opportunity'}`}><ArrowRight/></Link></li>;
}
